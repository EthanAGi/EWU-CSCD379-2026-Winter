name: Deploy API (Azure App Service)

on:
  push:
    branches: [ assignment3-submit ]
    paths:
      - "api/**"
      - ".github/workflows/deploy-api.yml"
  workflow_dispatch:

jobs:
  build_test_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # Restore BOTH the API and the test project
      - name: Restore
        run: |
          dotnet restore api/Assignment3.Api/Assignment3.Api.csproj
          dotnet restore api/Assignment3.Api.Tests/Assignment3.Api.Tests.csproj

      # Build solution (or at least both projects)
      - name: Build
        run: |
          dotnet build api/Assignment3.Api/Assignment3.Api.csproj -c Release --no-restore
          dotnet build api/Assignment3.Api.Tests/Assignment3.Api.Tests.csproj -c Release --no-restore

      # ✅ Run tests from the TEST PROJECT (not the API project)
      - name: Test
        run: dotnet test api/Assignment3.Api.Tests/Assignment3.Api.Tests.csproj -c Release --no-build

      - name: Publish
        run: dotnet publish api/Assignment3.Api/Assignment3.Api.csproj -c Release -o publish

      - name: Deploy
        uses: azure/webapps-deploy@v3
        with:
          app-name: assignment3
          publish-profile: ${{ secrets.AZURE_API_PUBLISH_PROFILE }}
          package: publish
